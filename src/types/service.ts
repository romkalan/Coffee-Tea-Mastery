export type TService = {
    id: string;
    title: string;
    type: string;
    price: number;
    time: string;
    format: string;
    description: string;
    image?: string;
    previewImage?: string;
    video?: string;
};
