import classNames from "classnames";
import styles from "./styles.module.scss";

function ChampionshipsPage() {
    return (
        <div className={classNames(styles.root)}>
            <h1>This is Championships Page</h1>
        </div>
    );
}

export default ChampionshipsPage;
